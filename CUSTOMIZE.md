# Как настроить приложение под себя

## 1. Изменить имя автора в футере

### Способ 1: Редактировать напрямую

Откройте файл `templates/base.html` и найдите строку (около строки 72):

```html
<p class="mb-0">© 2025 Разработано: <strong>Ваше Имя</strong></p>
```

Замените **"Ваше Имя"** на своё имя, например:

```html
<p class="mb-0">© 2025 Разработано: <strong>Иван Петров</strong></p>
```

### Способ 2: Через поиск и замену

В любом текстовом редакторе:
1. Откройте `templates/base.html`
2. Найдите текст: `Ваше Имя`
3. Замените на: `Ваше настоящее имя`
4. Сохраните файл

---

## 2. Изменить SECRET_KEY (рекомендуется для продакшена)

Откройте `app.py` и найдите строку (около строки 11):

```python
app.config['SECRET_KEY'] = 'your-secret-key-here-change-in-production'
```

Замените на случайную строку:

```python
app.config['SECRET_KEY'] = 'ваша-случайная-строка-минимум-24-символа'
```

### Как генерировать случайный ключ:

В Python консоли:
```python
import secrets
print(secrets.token_hex(32))
```

---

## 3. Добавить свои вопросы в викторину

### Вариант А: Через app.py

Откройте `app.py`, найдите функцию `init_db()` (около строки 192) и добавьте новые вопросы:

```python
Question(
    question_text='Ваш вопрос здесь?',
    option1='Первый вариант ответа',
    option2='Второй вариант ответа',
    option3='Третий вариант ответа',
    option4='Четвертый вариант ответа',
    correct_answer=1  # номер правильного ответа (1, 2, 3 или 4)
),
```

### Вариант Б: Использовать готовый скрипт

Запустите:
```bash
python add_questions.py
```

Это добавит еще 15 вопросов в вашу викторину.

### Вариант В: Создать свой скрипт

Создайте файл `my_questions.py`:

```python
from app import app, db, Question

with app.app_context():
    # Ваш вопрос 1
    q1 = Question(
        question_text='Столица России?',
        option1='Москва',
        option2='Санкт-Петербург',
        option3='Казань',
        option4='Екатеринбург',
        correct_answer=1
    )
    db.session.add(q1)
    
    # Добавьте больше вопросов...
    
    db.session.commit()
    print("Вопросы добавлены!")
```

Запустите:
```bash
python my_questions.py
```

---

## 4. Изменить тему викторины

Вы можете изменить вопросы на любую тему:
- История
- География
- Математика
- Литература
- Кино
- Музыка
- Спорт
- И т.д.

Просто замените вопросы в `app.py` или создайте новый скрипт (см. пункт 3).

---

## 5. Изменить название приложения

### В заголовке навигации:

Откройте `templates/base.html` и найдите (около строки 17):

```html
<a class="navbar-brand" href="{{ url_for('index') }}">Викторина</a>
```

Замените на:

```html
<a class="navbar-brand" href="{{ url_for('index') }}">Моя Крутая Викторина</a>
```

### В заголовке страницы:

В каждом шаблоне есть блок `{% block title %}`. Измените их по желанию.

---

## 6. Настроить реальный прогноз погоды

### Шаг 1: Получить API ключ

1. Зарегистрируйтесь на https://openweathermap.org/api
2. Перейдите в "API keys"
3. Скопируйте ваш API ключ

### Шаг 2: Установить переменную окружения

**Windows PowerShell:**
```powershell
$env:WEATHER_API_KEY="ваш_api_ключ"
```

**Windows CMD:**
```cmd
set WEATHER_API_KEY=ваш_api_ключ
```

**Linux/Mac:**
```bash
export WEATHER_API_KEY="ваш_api_ключ"
```

### Шаг 3: На PythonAnywhere

1. Перейдите во вкладку "Web"
2. Найдите раздел "Environment variables"
3. Добавьте:
   - Name: `WEATHER_API_KEY`
   - Value: `ваш_api_ключ`
4. Нажмите "Reload"

---

## 7. Изменить цветовую схему

Откройте `templates/base.html` и найдите раздел `<style>` (около строки 8).

### Изменить цвет навигационной панели:

Найдите:
```html
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
```

Замените `bg-primary` на:
- `bg-success` (зеленый)
- `bg-danger` (красный)
- `bg-warning` (желтый)
- `bg-info` (голубой)
- `bg-dark` (темный)

### Добавить свои стили:

В секции `<style>` добавьте:

```css
.navbar {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
}

.btn-primary {
    background-color: #667eea;
    border-color: #667eea;
}

.btn-primary:hover {
    background-color: #764ba2;
    border-color: #764ba2;
}
```

---

## 8. Добавить логотип

В `templates/base.html` замените:

```html
<a class="navbar-brand" href="{{ url_for('index') }}">Викторина</a>
```

На:

```html
<a class="navbar-brand" href="{{ url_for('index') }}">
    <img src="{{ url_for('static', filename='logo.png') }}" alt="Лого" height="30">
    Викторина
</a>
```

Создайте папку `static` и поместите туда файл `logo.png`.

---

## 9. Изменить количество баллов за ответ

В `app.py` найдите (около строки 159):

```python
current_user.total_score += 1
```

Измените на нужное количество баллов:

```python
current_user.total_score += 10  # теперь за правильный ответ 10 баллов
```

---

## 10. Сбросить базу данных

Если хотите начать с чистой базы:

1. Удалите файл `quiz.db`
2. Запустите `python app.py` или `python run_local.py`

База данных создастся заново с начальными вопросами.

---

## Примеры кастомизации

### Пример 1: Викторина по истории

1. Измените название: "Историческая Викторина"
2. Измените вопросы на исторические
3. Измените цветовую схему на тему истории (например, коричневый)

### Пример 2: Викторина по географии

1. Название: "Географический Квиз"
2. Добавьте вопросы о странах, столицах, континентах
3. Можно добавить карты в качестве подсказок

### Пример 3: Корпоративная викторина

1. Название компании в навигации
2. Вопросы о компании
3. Логотип компании
4. Корпоративные цвета

---

## Полезные ресурсы

- **Bootstrap документация**: https://getbootstrap.com/docs/5.3/
- **Flask документация**: https://flask.palletsprojects.com/
- **Генератор цветовых схем**: https://coolors.co/
- **Бесплатные иконки**: https://fontawesome.com/

---

Удачной кастомизации! 🎨

