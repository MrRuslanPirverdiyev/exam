# Быстрый старт

## Локальный запуск (для тестирования)

### Шаг 1: Установка зависимостей
```bash
pip install -r requirements.txt
```

### Шаг 2: Запуск приложения
```bash
python run_local.py
```
или
```bash
python app.py
```

### Шаг 3: Откройте браузер
Перейдите по адресу: http://127.0.0.1:5000

## Первые шаги в приложении

1. **Зарегистрируйтесь**:
   - Нажмите "Регистрация" в меню
   - Заполните форму (все поля обязательны)
   - Логин и имя должны быть уникальными

2. **Войдите в систему**:
   - Используйте созданные логин и пароль

3. **Проверьте погоду**:
   - На главной странице введите название города (например: Москва, Лондон, Париж)
   - Нажмите "Получить прогноз"
   - Увидите прогноз на 3 дня (демо-данные)

4. **Пройдите викторину**:
   - Нажмите "Викторина" в меню
   - Отвечайте на вопросы
   - За каждый правильный ответ получаете 1 балл
   - Вопросы задаются бесконечно в случайном порядке

5. **Посмотрите таблицу лидеров**:
   - Нажмите "Таблица лидеров"
   - Увидите топ-10 игроков по баллам

## Структура проекта

```
quiz_app/
│
├── app.py                    # Главный файл приложения Flask
├── requirements.txt          # Зависимости Python
├── run_local.py             # Скрипт для локального запуска
├── wsgi_example.py          # Пример WSGI для PythonAnywhere
│
├── templates/               # HTML шаблоны
│   ├── base.html           # Базовый шаблон (навигация, футер)
│   ├── index.html          # Главная страница (погода)
│   ├── register.html       # Регистрация
│   ├── login.html          # Вход
│   ├── quiz.html           # Викторина
│   └── leaderboard.html    # Таблица лидеров
│
├── README.md               # Полная документация
├── DEPLOY_INSTRUCTIONS.md  # Подробная инструкция по деплою
├── QUICK_START.md         # Этот файл
└── .gitignore             # Игнорируемые файлы для Git
```

## Тестовые данные

При первом запуске автоматически создается:
- База данных `quiz.db`
- 8 тестовых вопросов для викторины на тему IT/Web-разработки

## Деплой на PythonAnywhere

Для развертывания на хостинге смотрите файл `DEPLOY_INSTRUCTIONS.md`

## Кастомизация

### Изменить имя автора в футере
Откройте `templates/base.html` и найдите строку:
```html
<p class="mb-0">© 2025 Разработано: <strong>Ваше Имя</strong></p>
```
Замените "Ваше Имя" на своё имя.

### Добавить свои вопросы
Откройте `app.py` и найдите функцию `init_db()`.
Добавьте новые вопросы по шаблону:
```python
Question(
    question_text='Ваш вопрос?',
    option1='Вариант 1',
    option2='Вариант 2',
    option3='Вариант 3',
    option4='Вариант 4',
    correct_answer=1  # номер правильного ответа (1-4)
)
```

### Настроить реальный прогноз погоды
1. Получите бесплатный API ключ на https://openweathermap.org/api
2. Установите переменную окружения:
   ```bash
   # Windows (PowerShell)
   $env:WEATHER_API_KEY="ваш_ключ"
   
   # Windows (CMD)
   set WEATHER_API_KEY=ваш_ключ
   
   # Linux/Mac
   export WEATHER_API_KEY="ваш_ключ"
   ```

## Возможные проблемы

### Ошибка импорта модулей
```bash
pip install -r requirements.txt
```

### Порт 5000 уже занят
Измените порт в `run_local.py`:
```python
app.run(debug=True, host='0.0.0.0', port=5001)
```

### База данных не создается
Удалите файл `quiz.db` (если существует) и запустите заново:
```bash
python app.py
```

## Поддержка

Если возникли вопросы:
1. Проверьте README.md для полной документации
2. Смотрите DEPLOY_INSTRUCTIONS.md для деплоя
3. Проверьте консоль на наличие ошибок

---

Удачи! 🚀

